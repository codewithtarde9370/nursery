

const getHealth=(req,res) => {
   res.json(
    {
        success:true,
        message:"hello Tejasvi, U hav successfully moved function to controller folder and imported it in the index.js"
    }
)
}

export default getHealth